import React from 'react';
import {StyleSheet} from 'react-native';
import {TextInput} from 'react-native-paper';

const AppTextInput = ({...restProps}) => {
  return <TextInput {...restProps} />;
};

export default AppTextInput;
